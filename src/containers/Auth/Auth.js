import React from 'react';

const auth = (props) => (

    <div>
        <h1>Auth</h1>
        <button>LogIn with google</button>
        <button>Login with email</button>
    </div>

);

export default auth;